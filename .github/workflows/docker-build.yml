name: Docker build (PR check)

# Validates that the production image builds for the EC2 target arch (linux/amd64)
# on pull requests. Actual deploys happen in deploy.yml (on push to main / docker-ec2-deploy).

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image (linux/amd64)
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64
          push: false
          load: true
          tags: cineverse:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # --- Optional: push to Amazon ECR ---
  # Uncomment and set AWS_ROLE_ARN / AWS_REGION / ECR_REPOSITORY (repo secrets/vars)
  # to publish the image instead of building it on EC2.
  #
  #     permissions:
  #       id-token: write
  #       contents: read
  #     - uses: aws-actions/configure-aws-credentials@v4
  #       with:
  #         role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
  #         aws-region: ${{ vars.AWS_REGION }}
  #     - uses: aws-actions/amazon-ecr-login@v2
  #       id: ecr
  #     - uses: docker/build-push-action@v6
  #       with:
  #         context: .
  #         platforms: linux/amd64
  #         push: true
  #         tags: ${{ steps.ecr.outputs.registry }}/${{ vars.ECR_REPOSITORY }}:${{ github.sha }}
